<form method="post" action="{{ path('app_therapist_delete', {'id': therapist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ therapist.id) }}">
    <button class="btn">Delete</button>
</form>
